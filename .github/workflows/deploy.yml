name: FTP Deploy

on:
  push:
    branches: [main]

jobs:
  deploy:
    name: Deploy to FTP hosting
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 2

      # continue-on-error: pokud WEDOS zrovna nemá zapnutý vzdálený přístup
      # k MySQL (nebo je síť dole), tenhle krok spadne na timeoutu — ale
      # nesmí kvůli tomu zůstat celý deploy (FTP Deploy níže) nenasazený.
      # Failure kroku se pořád ukáže v Actions jako varování, jen
      # neblokuje.
      - name: Run pending DB migrations
        continue-on-error: true
        env:
          DB_HOST: ${{ secrets.DB_HOST }}
          DB_NAME: ${{ secrets.DB_NAME }}
          DB_USER: ${{ secrets.DB_USER }}
          DB_PASS: ${{ secrets.DB_PASS }}
        run: php scripts/run_migrations.php

      - name: Generate config.php
        env:
          DB_HOST: ${{ secrets.DB_HOST }}
          DB_NAME: ${{ secrets.DB_NAME }}
          DB_USER: ${{ secrets.DB_USER }}
          DB_PASS: ${{ secrets.DB_PASS }}
        run: |
          php -r '
          $config = [
              "db_host" => getenv("DB_HOST"),
              "db_name" => getenv("DB_NAME"),
              "db_user" => getenv("DB_USER"),
              "db_pass" => getenv("DB_PASS"),
          ];
          $php = "<?php\n"
              . "// Vygenerováno GitHub Actions při deployi z repozitářových secrets.\n"
              . "// Needituj ručně na serveru — příští deploy to přepíše. Necommituj do gitu.\n"
              . "return " . var_export($config, true) . ";\n";
          file_put_contents("config.php", $php);
          '

      - name: FTP Deploy
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: ${{ secrets.FTP_SERVER }}
          username: ${{ secrets.FTP_USERNAME }}
          password: ${{ secrets.FTP_PASSWORD }}
          protocol: ftps
          server-dir: ./
          exclude: |
            **/.git*
            **/.git*/**
            .github/**
            **/*.md
            node_modules/**
            database/**
            docs/**
            scripts/**
            setup-admin.php
